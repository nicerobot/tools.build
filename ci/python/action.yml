name: "python gate"
description: "ruff lint + format-check gate."
inputs:
  dir:
    description: "Directory holding the repo when it is checked out as a workspace subdirectory."
    required: false
    default: "."
runs:
  using: "docker"
  image: "docker://ghcr.io/nicerobot/tools.build/ci/python:v2.4.0"
